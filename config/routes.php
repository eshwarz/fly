<?php
// Match can be used to match the routing to an action of a controller;
// Router::match('profile', 'home#profile');

// Define a site root. 
Router::root('home#index');

// Router::match('welcome', 'home#index');
// Router::root('home#index');

?>